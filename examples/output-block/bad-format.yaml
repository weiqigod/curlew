# Intentionally invalid per M8-003 observable — do not add to TestSchema_examples walker.
# This file exercises the "unknown output format" error path (exit 3).
#
# Run: curlew run examples/output-block/bad-format.yaml
# Expected: exit 3, stderr contains 'unknown output format "markdown"'
name: Bad Format Demo
output:
  format: markdown
requests:
  - name: hello
    request:
      method: GET
      url: "https://httpbin.org/get"
