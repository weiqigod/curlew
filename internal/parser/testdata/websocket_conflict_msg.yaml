name: WebSocket Conflict Test
requests:
  - name: Bad Send
    request:
      protocol: websocket
      url: "ws://localhost:9999/ws"
      websocket:
        steps:
          - action: send
            message:
              type: ping
            message_template: "websocket_template.json"
