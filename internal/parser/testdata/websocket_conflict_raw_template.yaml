name: WebSocket Conflict raw+template Test
requests:
  - name: Bad Send
    request:
      protocol: websocket
      url: "ws://localhost:9999/ws"
      websocket:
        steps:
          - action: send
            message_raw: "PING"
            message_template: "websocket_template.json"
