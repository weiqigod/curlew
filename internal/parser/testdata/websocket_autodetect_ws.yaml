name: WebSocket Auto-detect WS
requests:
  - name: Echo WS
    request:
      url: "ws://localhost:9999/echo"
      websocket:
        steps:
          - action: send
            message:
              type: ping
