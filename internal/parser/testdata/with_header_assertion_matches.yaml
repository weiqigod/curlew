name: Header Assertion Matches
requests:
  - name: Check Header
    request:
      method: GET
      url: "https://example.com"
    assertions:
      headers:
        Content-Type:
          matches: "^application/.*"
