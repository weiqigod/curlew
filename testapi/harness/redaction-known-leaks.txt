# Known redaction leaks, as <format>:<value>.
#
# Every line here is a CURRENTLY FAILING surface, recorded so the harness can
# gate on regressions without the build being permanently red. The rules:
#
#   * a leak NOT listed here fails the harness — a new leak
#   * a listed leak that no longer leaks ALSO fails the harness — it was fixed,
#     so delete the line and let it be enforced from now on
#
# That second rule is the one that matters. A baseline nobody prunes becomes a
# permanent excuse; making a fix fail the harness forces the line out.
#
# ---------------------------------------------------------------------------
# Defect: redaction covers values on the way OUT but not on the way BACK.
#
# Measured against mudflat's /leak/* family:
#
#   request side:   "> Authorization: [REDACTED]"          — redacted
#   response side:  "got Bearer SENTINELVALUE123"          — not redacted
#
# The same sensitive value, extracted under a name that matches the §6.5
# heuristic, is replaced where curlew sent it and printed verbatim where the
# server returned it. CLI_SPECIFICATION §6.5 says the value is replaced in
# terminal output, JSON, TAP, JUnit, HTML, Markdown, event streams and JSONL
# logs, without restricting that to request-side occurrences.
#
# Practically: an API that echoes a token, a Set-Cookie carrying a session, or
# a redirect with a token in the query all end up in a CI log verbatim.
#
# The surfaces below are the ones this collection can currently reach. terminal,
# TAP, JUnit and JSONL are absent not because they are safe but because the
# assertions in 70-redaction.yaml all pass, so no `actual` value is printed. A
# failing assertion leaks in those too — see the minimal reproduction in
# testapi/gaps/expected-failures.yaml.
json:mud_tok_7f3a9c2e5b1d4680
json:mud_key_a1b2c3d4e5f60718
json:mud_sess_c9e4f1a7b2d80356
markdown:mud_tok_7f3a9c2e5b1d4680
markdown:mud_refresh_3b8c1f9e7a2d5460
markdown:mud_key_a1b2c3d4e5f60718
markdown:mud_sess_c9e4f1a7b2d80356
markdown:4242424242424242
events:mud_tok_7f3a9c2e5b1d4680
events:mud_refresh_3b8c1f9e7a2d5460
events:mud_key_a1b2c3d4e5f60718
events:mud_sess_c9e4f1a7b2d80356
events:4242424242424242
