# Tables still owed an executor.
#
# This is a debt register, not an exemption list. Every line is a table that
# states something checkable about the binary and that no test runs yet. A
# table declared genuinely unexecutable carries a marker in the document
# instead:  <!-- doc-check: table-not-executable <why> -->
#
# The file can only shrink. internal/docs/inventory_test.go fails when an
# unexecuted table is missing from this list (new debt), and fails again when a
# listed table has become executed and the line was not deleted (stale debt) —
# so the count cannot drift upward quietly, and paying one off is a diff.
#
# Same contract as testapi/harness/redaction-known-leaks.txt, which started as
# thirteen lines and emptied itself.
#
# Format: <document>\t<heading>\t<header cells joined by " | ">
CLI_SPECIFICATION.md	### 11.4 Rejected Constructions	Construction | Why | Exit
CLI_SPECIFICATION.md	### 18.8 `import openapi <spec>`	3.1 construct | Treatment
MANUAL.md	#### Where CEL appears	Site | Type | Example
MANUAL.md	### 4.1a Markdown response files	Body type | Rendering
