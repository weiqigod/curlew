# M30-001 — Executable documentation and agent usability

## Plan

1. Add failing checks for subcommand help, the manual's agent recipe, and clean UI assets.
2. Make help discovery safe and consistent; document the actual per-command output contract.
3. Refresh installation, agent onboarding, skill failure handling, and browser UI walkthrough.
4. Replace stale cookbook demonstrations with local Mudflat-backed examples; check displayed source against executable fixtures.
5. Audit current reference documents and label historical/platform material with its scope.
6. Build UI assets before binary/release compilation and test their presence and content.
7. Run the local Go gate, affected UI/site checks, and a skeptical diff review; record evidence.

## Boundaries

Use loopback fixtures for requests, signatures, retries, protocols, and load examples.
Provider integrations requiring credentials remain explicit configuration recipes; do not call paid services.
Historical task evidence stays historical. Do not claim remote integration proof from local fixtures.
Changes are prepared on work/M30-001-agent-docs-and-examples; no publication requested.

## Findings incorporated during execution

The agent topic files required replacement of unsupported YAML shapes, not only
wording changes. Final review also found standalone run dry-runs executed HTTP;
these now use the existing dependency-plan path. The coverage pass exposed
Mudflat control-pump frame interleaving; a yielding connection reproduced it and
whole-frame serialization fixes it without changing cookbook expectations.
