# M30-003 plan

1. Add failing CLI integration tests for project-local installation, idempotence,
   updates, conflict preservation, explicit targets and side-effect-free help.
2. Implement internal/skillinstall plus cmd/curlew/skill.go and help dispatch.
   Store hashes inside the installed skill; validate all destinations before writes.
   Preserve unowned files and fail before any content changes on conflicts.
3. Extend the existing skill with a linked authoring workflow. Keep legacy init
   scaffold compatible; document adopting legacy installs via identical-content
   checks and a temporary-directory diff for older or customized copies.
4. Add isolated behavioral evaluation fixtures and a reviewer rubric, with executable
   regression tests for the fixture evidence. Clearly separate deterministic checks
   from a real model evaluation (no paid model invocation in this task).
5. Update user docs, command specification and discovery references. Review changes,
   run scripts/ci-local.sh --go, then publish and merge under the established workflow.

Official discovery references checked 2026-09-16: OpenAI Build skills (repo
.agents/skills), Claude Code skills (.claude/skills), GitHub Copilot skills
(.github/skills). One canonical embedded skill; project-local targets only.

## Completion

All five steps completed. Static review and full-gate evidence are recorded in
management/reviews/M30-003-review.md and management/plans/M30-003-verified.md.
Behavior scenarios are delivered as a reproducible kit; no model execution or
automatic host-discovery success is claimed.
