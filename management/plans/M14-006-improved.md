# Improvement Report: M14-006

**Task:** CLI: curlew license --refresh + --debug + 8-code exit taxonomy
**Date:** 2026-05-05
**Review:** management/reviews/M14-006-review.md

---

## Iteration 1 Resolved Findings

| # | Severity | Finding | Fix Applied | Verified |
|---|----------|---------|------------|----------|
| 1 | High | DOD item 2 requires real binary invocation against stub for at least success(0), network(3), family-revoked(5), server-error(6), and device-not-registered(7) paths. Only the no-cache path was smoke-tested. | Extended `smoke/run.sh` with a stub-backed section: starts stub on port 19080, primes cache via `curlew login --no-browser`, then exercises exit 0 (success), 6 (5xx inject), 5 (AUTH_REFRESH_REUSED inject), 3 (stub stopped = dead port), and 7 (device.json removed) in sequence. | tests pass |
| 2 | Medium | `TestLicenseDebug_ShowsLastErrorType_FromRefresh` asserted `last_error_type` with `t.Logf` (non-failing). Behavior was effectively untested. | Changed assertion to `t.Errorf` so the test fails when `last_error_type` is empty after a known ProblemDetails failure. Also asserts the value contains expected refresh/expired context. | tests pass |
| 3 | Medium | `TestLicenseDebug_DecodesCachedJWT` did not assert the `kid` field despite behavior 8 requiring "active kid from the JWS header." | Added `if got := out["kid"]; got != "curlew-2025-01" { t.Errorf(...) }` to the assertion block. | tests pass |
| 4 | Medium | `CHANGELOG.md` was not updated. CLAUDE.md quality gate requires it before task completion. | Added M14-006 entry to `CHANGELOG.md` under `### Added` covering `--refresh` 8-code taxonomy, sentinels, `--debug` JSON output, MANUAL.md update, and smoke test coverage. | tests pass |
| 5 | Low | `refreshSentinelError.Error()` had 0% test coverage. | Added assertion in `TestRefreshTokens_AuthRefreshExpired_ReturnsSentinel`: `err.Error()` must be non-empty and contain the sentinel text "refresh token expired". | tests pass |

---

## Iteration 2 Resolved Findings (Review verdict: FAIL)

| # | Severity | Finding | Fix Applied | Verified |
|---|----------|---------|------------|----------|
| 1 | Critical | Smoke test non-idempotency: `stub-backend.sh stop` only killed `stub.pid`, leaving stale stub processes alive on port 19080. The exit-3 "dead port" check received exit 0 from the survivor instead of connection-refused. | Added belt-and-suspenders lsof kill in the `stop` case of `stub-backend.sh` (`lsof -ti tcp:${port} \| xargs kill -9 \|\| true`). Added pre-start port cleanup in the M14-006 smoke section of `smoke/run.sh` before starting a fresh stub. | ✓ tests pass |
| 2 | High | `licenseRefreshOut` saved `tokens.LicenseJWT` (empty string) to `license.json` on the flock-timeout / sibling-refresh path, corrupting offline verification. `tokensFromCache` returns `Tokens{RefreshToken: rt}` with empty `LicenseJWT` and `AccessToken`. | Added guard in `licenseRefreshOut`: if `tokens.LicenseJWT == ""` after a successful `RefreshTokens` call, skip the Save and return 0 (non-fatal; sibling already refreshed). Added `TestLicenseRefresh_EmptyLicenseJWT_DoesNotCorruptCache` to cover this path. | ✓ tests pass |

## Out of Scope (Deferred)

No findings deferred. All findings resolved.

## Quality Gate

| Check | Result |
|-------|--------|
| `go build ./cmd/curlew` | PASS |
| `go test ./...` | PASS |
| `golangci-lint run` | PASS |
| Coverage `cmd/curlew` | 81.5% |
| Coverage `internal/backend` | 84.4% |
| Coverage `internal/license` | 88.1% |

## Fix Commits

| Commit | Message | Findings Resolved |
|--------|---------|-------------------|
| f7253220 | test(license): tighten --debug and sentinel Error() assertions | iter1 #2, #3, #5 |
| 7eb4061e | test(smoke): extend M14-006 section with stub-backed binary invocations | iter1 #1 |
| 9e615b54 | docs(changelog): add M14-006 entry for license --refresh + --debug | iter1 #4 |
| 2e8e2a59 | fix(license): guard against empty LicenseJWT on sibling-refresh path | iter2 #2 |
| d68d1624 | fix(smoke): make stub stop idempotent; add pre-start port cleanup | iter2 #1 |

## Iteration 3 Resolved Findings (Review verdict: FAIL)

| # | Severity | Finding | Fix Applied | Verified |
|---|----------|---------|------------|----------|
| 1 | Low | `testdata/m14/stub.log` (generated by `stub-backend.sh start --bg`) was not listed in `.gitignore`, creating a risk of the runtime log being accidentally committed via `git add .`. | Added `testdata/m14/*.log` pattern to `.gitignore` with a comment describing the source. | ✓ tests pass, lint clean |

## Quality Gate (Final)

| Check | Result |
|-------|--------|
| `go build ./cmd/curlew` | PASS |
| `go test ./...` | PASS |
| `golangci-lint run` | PASS (0 issues) |
| Coverage `cmd/curlew` | 81.5% |
| Coverage `internal/backend` | 84.4% |
| Coverage `internal/license` | 88.1% |

## Summary

Iteration 1: 5/5 findings resolved. 0 deferred.
Iteration 2: 2/2 findings resolved. 0 deferred.
Iteration 3: 1/1 findings resolved. 0 deferred.
