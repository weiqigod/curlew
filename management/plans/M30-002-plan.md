# M30-002 plan

1. Reproduce gaps with a regression that runs the shipped local recipes.
2. Supply a loopback-only Python fixture for GET requests and metric POSTs.
3. Correct the negative output example, document project output inheritance,
   and replace directory-dependent/external plugin walkthrough commands.
4. Verify actual metric delivery, output JSON and no HTTP from the invalid case.
5. Review, run the full local Go gate, publish a PR and merge to synchronized main.

External provider configuration remains optional and explicitly unverified.
