id: M1-024
title: "Init command (curlew init)"
status: done
priority: 24
created: 2026-03-09
phase: "M1: Core CLI"
dependencies: [M1-003]
observable: |
  Run `./curlew init` in empty directory. Verify all files created. Run
  `./curlew run collections/sample.yaml` on the generated project.
behaviors:
  - "Given curlew init in an empty directory, when executed, then project structure is created with all scaffolding files"
  - "Given curlew init, when completed, then curlew.yaml contains project name and default config"
  - "Given curlew init, when completed, then .gitignore includes .env entry"
  - "Given curlew init, when completed, then .env.example documents available variables without real values"
  - "Given curlew init in a directory that already has curlew.yaml, when executed, then error indicates project already exists"
  - "Given the generated sample collection, when run with curlew run, then it executes successfully"
scope: |
  Add `init` command to CLI. Create template files. Generate working sample
  collection. Detect existing project.
definition_of_done:
  - All behavior tests pass
  - Observable output works as specified
  - Test coverage >= 80%
  - No build warnings or lint errors
  - Help text updated (if user-facing)
  - Smoke test updated (if new capability)
complexity: low
estimated_effort: "1-3 hours"
