# Code Review: M28-001

**Task:** Decide what src/ and web/ are, and make the repository say so
**Reviewer:** AI
**Date:** 2026-08-19
**Branch:** feature/M28-001-repository-shape

## Verdict: PASS

## Findings

None.

## Standards Compliance

| Category | Status | Notes |
|----------|--------|-------|
| Error Handling | PASS | `ErrNoLayoutTable`/`ErrNoTopLevelDirs` are proper sentinels, checked with `errors.Is` in tests; every wrap uses `fmt.Errorf("context: %w", err)` (`layout.go:123`, `layout.go:181`); both sentinels registered in a new `internal/docs/hints_init.go` that mirrors `internal/fuzzseed/hints_init.go`'s shape exactly, which is what made `TestCoverage_EverySentinelIsRegistered` (`internal/errors/coverage_test.go`) go green after an unplanned RED the plan itself documents finding mid-execution. No swallowed errors, no panics for expected failures. |
| Input Validation | PASS | `cellAt` returns `""` rather than panicking on a short row (covered by "a row with fewer cells than the header does not panic"); `normalizeLayoutDir` rejects a sub-path (`cmd/curlew/`) rather than silently normalising it, with a message naming the rule; `LayoutRows("")`/no-table/zero-row all resolve to `ErrNoLayoutTable` rather than an empty success; `TopLevelDirs` against a non-git directory and a directory with zero tracked/untracked entries both error rather than returning `nil, nil`. |
| Naming | PASS | No stuttering (`docs.LayoutRow`, not `docs.DocsLayout`); every exported symbol (`LayoutHeading`, `LayoutColumns`, `PlatformStatus`, `PlatformStatusDocs`, `ErrNoLayoutTable`, `ErrNoTopLevelDirs`, `LayoutRow`, `LayoutRow.String`, `LayoutRows`, `TopLevelDirs`, `LayoutAudit`, `LayoutAudit.Clean`, `AuditLayout`, and the new `Root` in `tables.go`) carries a doc comment; unexported helpers (`normalizeLayoutDir`, `cellAt`, `isLayoutPlaceholder`) are commented too, beyond what the standard requires. |
| Code Organization | PASS | Stays inside `internal/docs`; reuses `SplitRow`/`Column`/fence-and-heading conventions already established by `Inventory` rather than duplicating divergent logic; `TopLevelDirs` shells out to `git` with the same `exec.CommandContext` + `t.TempDir()` fixture pattern already used in `internal/prcheck`; no unused imports/vars (confirmed via `golangci-lint run` and `gofmt -l`, both clean). |
| Correctness | PASS | Verified directly, not just by reading: `go build ./...` clean; `golangci-lint run ./internal/docs/... ./internal/errors/...` reports 0 issues; `go test ./internal/docs/... -race -count=1` green; `go tool cover` shows every new function in `layout.go` and `hints_init.go` at 100% statement coverage. Reproduced both task-level behaviors live rather than trusting the plan's transcript: (1) `TestReadme_accounts_for_every_top_level_directory` passes as-is against the real README/repo; (2) creating an uncommitted `newthing_review_check/` directory and rerunning the same test fails it, naming exactly that directory, then passes again once removed — the working tree was left clean. `TestPlatformStatus_is_stated_by_every_document_that_must_agree` passes; independently confirmed the `PlatformStatus` string is verbatim-identical across `README.md`, `docs/TECH_CHOICES.md`, and the new `docs/SPECIFICATION.md` addendum by inspection of the diffs. Traced the `docs.Claims` phantom-claim avoidance (the `const readmeFile` trick) through `claims.go`'s AST walk by hand: `splitArgs` only collects `*ast.BasicLit` args of a call's own arg list, so neither the `readmeFile` identifier nor the `"README.md"` literal buried two calls deep inside `filepath.Join(dir, "README.md")` in the `TopLevelDirs` fixtures register a claim — confirmed no new debt in `TestDocTables_everyTableIsExecutedOrDeclaredProse`'s "0 owed an executor" output. |
| Test Quality | PASS | Table-driven throughout, `t.Run` with descriptive names; both error paths and edge cases covered (fences, wrong heading, separator row, short row, sub-path rejection, empty table, zero directories, non-repo root, staged-but-uncommitted, untracked-but-unignored, gitignored); mutation-labelled cases for every `LayoutAudit` field in both directions (`Unaccounted`/`Stale`/`Unexplained`/`Duplicate`), matching the house convention set by `TestProse_register_cannot_grow`; a real git repo fixture in `t.TempDir()` for `TopLevelDirs`, mirroring `internal/prcheck/git_test.go`; the two task observables are each exercised by an integration-level test against the real checkout, not just against synthetic fixtures. |

## Test Coverage
- `internal/docs` package: 85.1% (unchanged areas already covered pre-existing at similar levels; the package's older files carry the bulk of untested lines, not this change).
- New code (`layout.go`, `hints_init.go`): 100% statement coverage on every added function, confirmed via `go tool cover -func`.
- Missing coverage: none in the changed files.

## Summary

The implementation matches its plan closely and the plan's own "Execution results" transcript checks out under independent reproduction: the pre-audit gate (`./scripts/ci-local.sh --go`) passes clean, `golangci-lint` and `gofmt` are silent, every new function has full statement coverage, and both of the task's stated behaviors were reproduced live (not just re-read) — including physically creating and removing an unaccounted-for directory to watch the guard fire and clear. Error handling, naming, and code organization all follow the package's existing conventions (`SplitRow`/`Column`/fence-tracking reuse, the `hints_init.go` sentinel-registration pattern, `LayoutAudit`/`AuditLayout` mirroring `ProseAudit`/`AuditProse`) rather than inventing new ones. The one thing worth naming without it being a code-review finding: the task's DoD line "Decision recorded by the project owner" is met in substance but not in strict authorship — `docs/TECH_CHOICES.md`'s new "Repository Shape" section, the CHANGELOG entry, and the plan all say plainly that an automated pipeline made the B-vs-A-vs-C call on stated grounds, with A and C left open for the real owner to revisit — and the DoD's `./scripts/ci-local.sh --full` line is unmet for a reason the plan documents with before/after evidence (an E2E `test-stack up` timeout on this host, byte-identical whether or not this branch's changes are present, and outside `/review`'s own gate per this skill's Step 0). Both are transparently disclosed rather than hidden, and neither is a Go-standards defect, so neither is recorded as a Finding here; they are `/verify`'s and the project owner's to close.
